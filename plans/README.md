# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, run every verification command, commit/describe the jj change as instructed in the plan, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Ignore root build artifacts | P1 | S | — | DONE |
| 002 | Make hook-state tests time-independent | P1 | S | — | DONE |
| 003 | Optimize JSONL last-timestamp reads | P2 | S | 002 | DONE |
| 004 | Consolidate small duplicated utilities | P3 | S | 002 | DONE |
| 005 | Declare fuzzy as a direct dependency | P3 | S | 002 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 is independent and intentionally first because the current checkout has untracked root binaries `glint` and `glint-perf` that make `jj status` noisy.
- 002 restores `go test ./...`; run it before plans 003-005 so their verification gates are meaningful.
- 003, 004, and 005 are independent of each other after 002.

## Selected findings

- DX: root build artifacts are not ignored (`.gitignore`, `Makefile`).
- Tests: hook-state fixture timestamps aged past the 14-day retention cutoff (`internal/agent/store_test.go`, `internal/agent/store.go`).
- Performance: `lastJSONLTimestamp` scans full JSONL files to read the final timestamp (`internal/agent/agent.go`).
- Tech debt: duplicated tiny helpers should move to `internal/util`.
- Dependencies: `github.com/sahilm/fuzzy` is directly imported but marked indirect in `go.mod`.

## Findings considered and rejected or deferred

- JJ cleanup dirty-check/session-kill ordering: higher impact but more behavior-sensitive; deferred because the user selected minimal changes.
- Hook payload minimization/file locking: important but not minimal; deferred.
- Refresh-loop tmux batching and palette caching: useful performance work but broader than the selected set; deferred.
- `internal/ui/ui.go` god-object refactor: real architecture debt, but too large/risky for this batch.

## Execution note

This repository is a Jujutsu checkout rather than a Git repository. The improve skill's `execute <plan>` flow normally requires isolated Git worktrees, so these plans include jj workflow instructions for external executors instead of assuming git branches.
